version: '3.8'

services:
  db:
    image: mysql:5.7
    container_name: api-db-1
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3307:3306" 
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: WhE090807
      MYSQL_DATABASE: Prisma
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: